These are my custom dotfiles for vimrc and bashrc 

To use them:
    use the --version command to ensure you have vim and bash installed
    git clone the git repository from github:psegynola
    cd into folder
    execute ./setup
